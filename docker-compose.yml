version: "3.9"

services:
  pod-b:
    build: .
    container_name: pod_b
    env_file:
      - .env
    ports:
      - "8001:8000"
    networks:
      - crm-pod-a_default   # 👈 this matches Pod A’s network
    depends_on:
      - db                  # only if you run your own db

networks:
  crm-pod-a_default:
    external: true          # 👈 tells Docker: use Pod A’s existing network
